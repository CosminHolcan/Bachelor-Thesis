import { mergeStyles } from "@fluentui/react";

export const styleStack: string = mergeStyles({
    position: "absolute",
    marginTop: "0px"
});

export const styleContentArea: string = mergeStyles({
    position: "relative",
    width: "100vw"
});